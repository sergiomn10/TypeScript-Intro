const name: string = "Sergio";
let hpPoints: number | "FULL" = 95; // el simbolo de tubertia | indica que puedes darle mas opcines de datos a la variable
const isAlive: boolean = true;

hpPoints = "FULL";

console.log({
  name,
  hpPoints,
  isAlive,
});

export {};
